{
  "name": "primary",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/analytics.js --bundle --outfile=dist/analytics.js --format=iife",
    "dev": "esbuild src/analytics.js --bundle --outfile=dist/analytics.js --format=iife --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jvissup/pokestats-v3.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/jvissup/pokestats-v3/issues"
  },
  "homepage": "https://github.com/jvissup/pokestats-v3#readme",
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
